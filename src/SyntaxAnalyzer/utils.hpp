#ifndef UTILS_HPP
#define UTILS_HPP

#include<string>
#include<vector>
#include<unordered_map>
#include<unordered_set>
#include<fstream>
#include<iostream>
#include<filesystem>

enum TokenType {
    KEYWORD,
    SYMBOL,
    IDENTIFIER,
    INT_CONST,
    STRING_CONST
};

#endif